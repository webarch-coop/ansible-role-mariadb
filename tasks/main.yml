---
- name: mariadb-server and mariadb-client packages present
  apt:
    pkg: "{{ item }}"
    state: latest
    update_cache: yes
  with_items:
    - mariadb-server
    - mariadb-client
    - mysqltuner
  tags:
    - mariadb

