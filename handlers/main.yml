---
- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: true
  listen: Reload systemd
  tags:
    - mariadb

- name: MariaDB restarted
  ansible.builtin.systemd:
    name: mysql
    state: restarted
  listen: Restart mariadb
  tags:
    - mariadb
...
