---
# https://github.com/ansible/schemas/issues/386
# ERROR! 'version' is not a valid attribute for a RoleMetadata
# version: 2
galaxy_info:
  role_name: mariadb
  author: Chris Croome
  namespace: chriscroome
  description: Webarchitects Ansible role for installing and configuring MariaDB on Debian servers
  company: Webarchitects Co-operative
  license: GNU General Public License v3.0 (GPLv3)
  min_ansible_version: "2.10"
  platforms:
    - name: debian
      versions:
        - bookworm
        - bullseye
        - buster
        - stretch
  galaxy_tags:
    - debian
    - mariadb
dependencies: []
# List your role dependencies here, one per line. Be sure to remove the '[]' above,
# if you add dependencies to this list.
...
